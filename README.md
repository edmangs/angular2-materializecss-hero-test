# angular2-materializecss-hero-test

Se implementa el framework de angular2 con el ejemplo de hero, mas se usa materializecss para el diseño

##Pre conditions
  1. Install npm

##Install app

  **execute**
  ```
    1. npm install
    2. npm run typings install
    3. npm start
  ```
